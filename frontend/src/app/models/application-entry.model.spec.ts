import { ApplicationEntry } from './application-entry.model';

describe('ApplicationEntry', () => {
  it('should create an instance', () => {
    expect(new ApplicationEntry()).toBeTruthy();
  });
});
